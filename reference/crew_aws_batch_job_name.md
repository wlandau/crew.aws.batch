# Terminate an AWS Batch job.

Not a user-side function. For internal use only.

## Usage

``` r
crew_aws_batch_job_name(name)
```

## Arguments

- name:

  Character string, an AWS Batch job name, possibly invalid.

## Value

Character string, a valid AWS Batch job name.
